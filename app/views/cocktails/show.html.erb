<!-- afficher le nom du cocktail + creer le form pour y mettre une dose (ingredient + description(de la dose)) -->
<h1>Here is your <%= @cocktail.name %> reciept</h1>

<% @cocktail.doses.each do |dose| %>
  <p> <%= dose.description %> / <%= dose.ingredient.name %></p>
  <%= link_to "Delete", dose_path(dose), method: :delete, data: {confirm: "Are you sure?"} %>
<% end %>

<div>
<p></p>
<%= link_to "Add a dose", new_cocktail_dose_path(@cocktail), class: "btn btn-primary" %>
<%= link_to "Go back to your cocktails list", cocktails_path(@cocktail) %>
</div>

